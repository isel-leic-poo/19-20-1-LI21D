package model;

/**
 * Represents the junk piles in the Robots game.
 */
public class JunkPile extends Actor {

    /**
     * Initiates the junk piles at the given coordinates
     * @param x The vertical coordinate
     * @param y The horizontal coordinate
     */
    public JunkPile(int x, int y) {
        super(x, y);
    }
}
